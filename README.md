# Angry Birds 

**Angry Birds** — логическая игра, в которой игрок запускает птиц с уникальными способностями, стараясь разрушить постройки и уничтожить свиней. В этом проекте отражены основные принципы ООП и архитектура на Rust с использованием структур и трейтов.

---

## 🕹️ Классы и модули игры

**Bird (`bird.rs`)**
- Описывает птицу: цвет, размер, вес, скорость, способность.
- Методы: полёт, столкновение, активация способности.

**Ability (`ability.rs`) (Trait)**
- Интерфейс способности птицы: activate, deactivate, name.
- Примеры: ускорение, взрыв, разделение.

**Pig (`pig.rs`)**
- Свинья: здоровье, размер.
- Методы: получение урона, проверка уничтожения.

**StructureBlock (`structure_block.rs`)**
- Блок конструкции: материал, прочность.
- Методы: получение урона, разрушение.

**Targetable (Trait)**
- Универсальный интерфейс — получение урона и проверка разрушения.

**Level (`level.rs`)**
- Уровень: содержит набор птиц, свиней и блоков.
- Методы: старт уровня, проверка победы.

---

## 🎮 Кратко о Gameplay

В Angry Birds отсутствует единый способ прохождения уровня — игрок выбирает стратегию и порядок решения задач сам, используя разные типы птиц и их индивидуальные способности.

---

## ❓ 15 вопросов Junior Rust Developer

1. **Что такое ownership и borrowing и почему это важно?**  
   Ownership — уникальное владение ресурсом, гарантирующее автоматическое освобождение памяти и предотвращающее утечки. Borrowing — это заимствование ссылки на данные без передачи владения, позволяющее безопасно и эффективно использовать память без гонок и лишних затрат.

2. **Что такое lifetime в Rust и для чего он нужен?**  
   Lifetime — время жизни ссылки. Помогает компилятору гарантировать, что ссылка всегда валидна и не указывает на уже освобождённую область памяти. Это защищает от висячих указателей и ошибок управления памятью.

3. **Как Rust обрабатывает ошибки? Чем Result отличается от panic?**  
   Для прогнозируемых (ожидаемых) ошибок используется тип Result, который требует явной обработки. Panic! применяется для критических, неожиданных ситуаций и завершает работу программы. Такой подход делает код надёжным и предсказуемым.

4. **Что такое trait в Rust и как он используется?**  
   Trait — это описание набора методов, которые должен реализовать тип. Это аналог интерфейса в других языках и основной способ достижения абстракции и полиморфизма в Rust.

5. **Как создать структуру и методы к ней в Rust?**  
   Структура задаёт набор полей для хранения данных. Методы реализуются через специальный блок, что позволяет инкапсулировать поведение.

6. **Что такое enum и где его применять?**  
   Enum — это тип данных с несколькими вариантами (альтернативами), используемый для представления вариативных или ограниченных наборов значений, например, состояния, команды, результаты функций.

7. **Как работает модульная система Rust?**  
   Код делят на модули с помощью ключевого слова `mod`. Модули структурируют проект, улучшают повторное использование, чтение и поддержку кода. Публичные элементы явно помечаются как `pub`.

8. **Объясните, что такое unsafe код.**  
   Unsafe-код снимает ограничения и гарантии Rust по безопасности, давая программисту ручной контроль над памятью, указателями и FFI. Используется только там, где это действительно необходимо.

9. **Чем отличаются Box, Rc и Arc?**  
   Box используется для единственного владения и хранения данных в heap. Rc предоставляет разделение владения в однопоточной среде с подсчётом ссылок. Arc — аналог Rc для работы в многопоточной среде, обеспечивает потокобезопасность.

10. **Что такое async/await в Rust?**  
    Это синтаксис для асинхронного программирования: позволяет писать неблокирующий код, выполнять операции параллельно и эффективно работать с I/O.

11. **Как реализуются принципы ООП (инкапсуляция, наследование, полиморфизм) в Rust?**  
    Инкапсуляция — достигается приватностью полей и методов. Классического наследования нет, вместо него композиция и traits. Полиморфизм реализуется через trait objects (динамический диспетчер).

12. **Чем структуры в Rust отличаются от классов в ООП языках?**  
    Структуры хранят только данные, не поддерживают наследование. Методы реализуются отдельно через блоки, а поведение задаётся через трейты и композицию.

13. **Как реализуются интерфейсы и абстрактные классы?**  
    Интерфейсы реализуются через traits: это набор методов, который различные типы обязаны реализовать. Абстрактных классов в Rust нет, роль абстракции выполняют traits.

14. **Почему в Rust нет классического наследования?**  
    Rust использует композицию и traits вместо наследования. Это снижает сложность, избегает проблем множественного наследования и избыточности иерархий.

15. **В чем преимущества подхода Rust к ООП перед другими языками?**  
    Такой подход более гибкий и безопасный: компилятор строго следит за памятью, нет гонок данных и утечек. Композиция и traits делают код выразительным и легко поддерживаемым.

---

## Структура проекта

```
angrybirds-rust/
├── src/
│   ├── birds.rs
│   ├── ability.rs
│   ├── pig.rs
│   ├── structure_block.rs
│   ├── level.rs
│   └── lib.rs
├── README.md
├── Cargo.toml
```

---

